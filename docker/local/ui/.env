# ===== Host에서 접속할 Web UI 포트 =====
KAFKA_UI_PORT=8081          # Kafka UI (container: 8080)
ADMINER_PORT=8082           # Adminer  (container: 8080)
REDISINSIGHT_PORT=8083      # RedisInsight v2 (container: 5540)
DYNAMODB_ADMIN_PORT=8084    # dynamodb-admin (container: 8001)

# ===== UI 컨테이너에서 백엔드로 접근할 주소 (같은 도커 네트워크 기준) =====
# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
ZOOKEEPER_CONNECT=zookeeper:2181

# MySQL
MYSQL_HOST=mysql
MYSQL_PORT=3306

# LocalStack (DynamoDB)
DYNAMODB_ENDPOINT=http://localstack:4566
AWS_REGION=ap-northeast-2

# 공통
TZ=Asia/Seoul
